<h1><%= @user.name %>のページ</h1>
<%= link_to_if(current_user == @user, 'プロフィールの編集はこちら', root_path){} %>
<br />

<div class="row">
  <div class="col s12">
    <ul class="tabs">
      <li class="tab col s3"><a href="#test1">参加</a></li>
      <li class="tab col s3"><a class="active" href="#test2">開催</a></li>
      <li class="tab col s3"><a href="#test3">お気に入り</a></li>
    </ul>
  </div>
  <div id="test1" class="col s12">
    <% @user.participant_events.each do |event| %>
      <%= link_to event.title, event_path(event) %><br />
    <% end %>
  </div>

  <div id="test2" class="col s12">
    <% @user.events.each do |event| %>
      <%= link_to event.title, event_path(event) %><br />
    <% end %>
  </div>

  <div id="test3" class="col s12">
    <% @user.favorite_events.each do |event| %>
      <%= link_to event.title, event_path(event) %><br />
    <% end %>
  </div>
</div>
