<% if current_user.active_relationships.find_by(followed_id: user.id) %>
  <%= link_to 'フォローしない', relationship_path(current_user.active_relationships.find_by(followed_id: user.id)), method: :delete, remote: true, class: "btn waves-effect waves-light"%>
<% else %>
  <%= link_to 'フォローする', relationships_path(followed_id: user.id), method: :post, remote: true, class: "btn waves-effect waves-light" %>
<% end %>
